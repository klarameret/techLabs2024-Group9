import { Kanit } from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const kanit = Kanit({ subsets: ['latin'], weight: "600" });
export const lusitana = Lusitana({ subsets: ['latin'], weight: "400" });